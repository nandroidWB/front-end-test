import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#10b981',  // El color verde que estabas usando
    },
  },
  // Puedes personalizar más aspectos del tema aquí
});

export default theme;